version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000" # Maps container port 3000 to host port 3000
    environment:
      NODE_ENV: production
    restart: always
